# BioKey Evaluation Report

This report is generated by:

```bash
ruby tools/evaluate_dataset.rb
```

Run the command to refresh this file with current FAR/FRR metrics based on labeled rows in `biometric_attempts`.
